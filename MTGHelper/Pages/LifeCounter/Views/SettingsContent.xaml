<?xml version="1.0" encoding="utf-8" ?>
<ContentView
    x:Class="MTGHelper.Views.SettingsContent"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModels="clr-namespace:MTGHelper.ViewModels">
    <HorizontalStackLayout>
        <Button Command="{Binding ResetGameCommand}" Text="Reset" />
        <Button Command="{Binding ChangeSettingsToPlayersCommand}" Text="Players" />
        <Button Text="Life" />
        <Button Text="Settings" />
        <Button Text="Dice" />
    </HorizontalStackLayout>
</ContentView>
